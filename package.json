{
    "name": "video-ends-at",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "devDependencies": {
        "sharp": "0.34.5"
    },
    "peerDependencies": {
        "typescript": "^6.0.2"
    },
    "scripts": {
        "dev": "bun build --outdir=./dist ./src/index.ts --watch --minify",
        "dist": "bun ./src/copyToDist.ts && bun run build",
        "build": "bun build --outdir=./dist ./src/index.ts --minify"
    }
}